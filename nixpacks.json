{
  "providers": ["node"],
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs_22", "pnpm-9_x"]
    },
    "install": {
      "cmds": ["pnpm install --frozen-lockfile"]
    },
    "build": {
      "cmds": ["pnpm run build"]
    }
  },
  "start": {
    "cmd": "node server.js"
  }
}
