# Force Node.js build instead of static site detection
[phases.setup]
nixPkgs = ["nodejs-22", "pnpm"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = ["pnpm run build"]

[start]
cmd = "node server.js"